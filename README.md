# Bank Loan Approval Prediction

Loan approval prediction using **XGBoost** with **SHAP waterfall explanations**.

The system applies a policy rule (CIBIL cutoff), predicts loan approval or rejection, and explains each decision using explainable AI.

---

## Requirements

Install the required Python packages:

```bash
pip install -r requirements.txt


##How to Run
git clone https://github.com/jaykrishnan43/loan-approval-project.git
cd loan-approval-project
pip install -r requirements.txt
streamlit run app/streamlit_app.py


Live App: https://loan-approval-project1.streamlit.app/
